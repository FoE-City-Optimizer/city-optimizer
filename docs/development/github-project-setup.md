# Existing GitHub Project integration

Repository: `FoE-City-Optimizer/city-optimizer`; existing organization Project: **Forge of Empires City Optimizer — Engineering** (#1). Do not recreate either. The Project has Status options `Backlog`, `Needs Refinement`, `Ready`, `In progress`, `In review`, `Blocked`, `Done`; spelling/case in GitHub is authoritative. It has Priority, Size, Estimate, Risk, Workstream, parent/sub-issue and linked-PR fields. Before bootstrap, no milestones or Project items existed. Issue Types `Task`, `Bug`, `Feature`, `Epic`, `Research` are enabled.

Bootstrap added [M0 settings #1](https://github.com/FoE-City-Optimizer/city-optimizer/issues/1), [M1 Epic #2](https://github.com/FoE-City-Optimizer/city-optimizer/issues/2), M1 Research [#3](https://github.com/FoE-City-Optimizer/city-optimizer/issues/3) and [#4](https://github.com/FoE-City-Optimizer/city-optimizer/issues/4), and [M2 Epic #5](https://github.com/FoE-City-Optimizer/city-optimizer/issues/5). All five were verified in Project #1; #3 and #4 are native sub-issues of #2, #4 is blocked by #3, and #5 is blocked by #2.

Use Research for evidence gathering, Epic for an outcome, and Task for a bounded implementation. New discovery work should start in `Backlog` or `Needs Refinement`; only refinement may mark an implementation Issue `Ready` after dependencies and acceptance criteria are complete. Use native sub-issues/dependencies. Confirm an Issue appears in Project #1 after creation; auto-add is owner-reported but must be verified for each batch. Prefer `gh project` first-class commands. Project configuration is owner-controlled and should be changed additively only after inspection.

The `Priority` and `Effort` field options were not exposed by the CLI field-list response; use verified options only. `Size` has XS/S/M/L/XL, Risk has Low/Medium/High, and Workstream has Extension, Contracts & Domain, Solver, API & Platform, Web & UX, Infrastructure & CI, Security. CLI-discovered views are `Current iteration`, `Next iteration`, `Prioritized backlog`, `Roadmap`, `In review`, `My items`, `Delivery Board`, `Ready Queue`, and `Discovery & Refinement`. Auto-add workflows report enabled and the first five Issues appeared, but the exact filter still needs owner UI inspection. The Project is private while the repository is public; confirm that this is intended. See [owner checklist](../project/owner-actions.md).
