# Issue #40 — pre-installation privacy and security review request

**Review status:** pending Security & Privacy role review. The exact prototype has **not** been installed or run on the owner's city. This checklist is the Research Agent's pre-review inventory, not an approval to install.

**Files in scope:** `docs/research/issue-40-chrome-probe/{manifest.json,main.js,bridge.js,check-probe.mjs,README.md}`. The local SVG replay is generated only from the separately reviewed #24 artifact; its derivative output is kept in the local visualization workspace, not committed as a new fixture.

| Boundary or risk        | Current control                                                                                                                                                                                                    | Remaining review question                                                                                                  |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| Host and frame          | One exact HTTPS game-world match; both scripts reject another origin/path and child frames. No extension permissions.                                                                                              | Confirm the owner still authorizes this world and that the match is narrow enough.                                         |
| Network response        | MAIN sees only completed same-origin `/game/json` XHR. It selects two named service entries and never pauses, changes, saves, or transmits a response.                                                             | A text response is transiently JSON parsed in page memory; confirm this is acceptable under the no-raw-retention boundary. |
| Data minimization       | MAIN keeps selected ID/type/position pairs in memory for an update comparison; only bounded counts/status cross `postMessage`. No metadata URL, query, cookie, header, account/social/chat, or raw object crosses. | Confirm whether even temporary source IDs are acceptable for this research goal.                                           |
| Untrusted page boundary | ISOLATED checks exact message keys, value enums, numeric bounds, origin, source window, and event limit. It displays only text with `textContent`.                                                                 | Page scripts can forge structurally valid events; no trust or completeness claim follows from the panel.                   |
| Resource and lifecycle  | Maximum 10,000 selected entries per array, 32 emitted diagnostics, 6 MB cap for text response parsing, no storage. Owner removal plus reload restores unwrapped XHR and removes panel.                             | Review memory/CPU cost during a game reload and any unobserved fetch transport.                                            |
| Gameplay                | No requests, UI clicks, mutation of game payloads, or game commands. `XMLHttpRequest` wrappers call original methods.                                                                                              | Verify passive wrapping does not materially disrupt the page.                                                              |
| Public artifacts        | Reviewed #24 fixture bytes are hash checked; generated local SVG is a derivative of those bytes and can reveal the recognizable layout.                                                                            | Confirm no SVG/PNG should be committed or shared beyond the owner without exact-artifact review.                           |

**Research Agent recommendation:** keep the probe uninstalled until Security & Privacy reviews these exact files. If approved, observe one startup and one owner-performed representative change, then remove it and reload. Reject unknown service shapes; record only aggregate diagnostics. The live map and any publication of a new city fixture remain separate gates.
